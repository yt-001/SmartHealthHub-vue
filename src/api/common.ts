// src/api/common.ts

import request from './http'

/**
 * 此文件用于存放不属于特定业务模块的通用 API。
 * 例如：文件上传、获取全局配置等。
 */

// 示例：获取全局配置
// export const getGlobalConfig = () => {
//   return request.get('/config/global')
// }