# 文章分类与视频分类管理接口文档

## 1. 文章分类管理接口

### 1.1 分页查询文章分类

#### 接口地址
```
POST /article-categories/page
```

#### 请求方式
POST

#### 请求参数 (PageParam<ArticleCategoriesQuery>)
| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| pageNum | integer | 否 | 当前页码，默认为1 |
| pageSize | integer | 否 | 每页条数，默认为10 |
| query | object | 否 | 查询条件对象 |

##### query对象结构
| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| name | string | 否 | 分类名称（模糊查询） |
| isEnabled | integer | 否 | 是否启用：0 否 1 是 |

#### 返回结果 (ResultBean<PageBean<ArticleCategoriesVO>>)
```json
{
  "code": 200,
  "message": "success",
  "data": {
    "pageNum": 1,
    "pageSize": 10,
    "total": 100,
    "pages": 10,
    "list": [
      {
        "id": 1001,
        "name": "健康饮食",
        "description": "关于营养搭配、健康饮食习惯等方面的文章",
        "sortOrder": 1,
        "isEnabled": 1
      }
    ]
  }
}
```

---

### 1.2 获取所有启用的文章分类（简化信息）

#### 接口地址
```
GET /article-categories/simple-list
```

#### 请求方式
GET

#### 请求参数
无

#### 返回结果 (ResultBean<List<CategorySimpleVO>>)
```json
{
  "code": 200,
  "message": "success",
  "data": [
    {
      "id": 1001,
      "name": "健康饮食"
    }
  ]
}
```

---

### 1.3 获取所有启用的文章分类

#### 接口地址
```
GET /article-categories/enabled-list
```

#### 请求方式
GET

#### 请求参数
无

#### 返回结果 (ResultBean<List<ArticleCategoriesVO>>)
```json
{
  "code": 200,
  "message": "success",
  "data": [
    {
      "id": 1001,
      "name": "健康饮食",
      "description": "关于营养搭配、健康饮食习惯等方面的文章",
      "sortOrder": 1,
      "isEnabled": 1
    }
  ]
}
```

---

### 1.4 根据ID获取文章分类详情

#### 接口地址
```
GET /article-categories/{id}
```

#### 请求方式
GET

#### 请求参数
| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| id | long | 是 | 分类ID |

#### 返回结果 (ResultBean<ArticleCategoriesVO>)
```json
{
  "code": 200,
  "message": "success",
  "data": {
    "id": 1001,
    "name": "健康饮食",
    "description": "关于营养搭配、健康饮食习惯等方面的文章",
    "sortOrder": 1,
    "isEnabled": 1
  }
}
```

---

### 1.5 创建文章分类

#### 接口地址
```
POST /article-categories/create
```

#### 请求方式
POST

#### 请求参数 (ArticleCategoriesCreateDTO)
| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| name | string | 是 | 分类名称 |
| description | string | 否 | 分类描述 |
| sortOrder | integer | 否 | 排序 |
| isEnabled | integer | 否 | 是否启用：0 否 1 是，默认为1 |

#### 返回结果 (ResultBean<String>)
```json
{
  "code": 200,
  "message": "分类创建成功",
  "data": null
}
```

---

### 1.6 更新文章分类

#### 接口地址
```
PUT /article-categories/update
```

#### 请求方式
PUT

#### 请求参数 (ArticleCategoriesUpdateDTO)
| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| id | long | 是 | 分类ID |
| name | string | 否 | 分类名称 |
| description | string | 否 | 分类描述 |
| sortOrder | integer | 否 | 排序 |
| isEnabled | integer | 否 | 是否启用：0 否 1 是 |

#### 返回结果 (ResultBean<String>)
```json
{
  "code": 200,
  "message": "分类更新成功",
  "data": null
}
```

---

### 1.7 删除文章分类

#### 接口地址
```
DELETE /article-categories/delete/{id}
```

#### 请求方式
DELETE

#### 请求参数
| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| id | long | 是 | 分类ID |

#### 返回结果 (ResultBean<String>)
```json
{
  "code": 200,
  "message": "分类删除成功",
  "data": null
}
```

---

## 2. 视频分类管理接口

### 2.1 分页查询视频分类

#### 接口地址
```
POST /video-categories/page
```

#### 请求方式
POST

#### 请求参数 (PageParam<VideoCategoriesQuery>)
| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| pageNum | integer | 否 | 当前页码，默认为1 |
| pageSize | integer | 否 | 每页条数，默认为10 |
| query | object | 否 | 查询条件对象 |

##### query对象结构
| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| name | string | 否 | 分类名称（模糊查询） |
| isEnabled | integer | 否 | 是否启用：0 否 1 是 |

#### 返回结果 (ResultBean<PageBean<VideoCategoriesVO>>)
```json
{
  "code": 200,
  "message": "success",
  "data": {
    "pageNum": 1,
    "pageSize": 10,
    "total": 100,
    "pages": 10,
    "list": [
      {
        "id": 2001,
        "name": "健身教学",
        "description": "各类健身动作教学视频",
        "sortOrder": 1,
        "isEnabled": 1
      }
    ]
  }
}
```

---

### 2.2 获取所有启用的视频分类（简化信息）

#### 接口地址
```
GET /video-categories/simple-list
```

#### 请求方式
GET

#### 请求参数
无

#### 返回结果 (ResultBean<List<CategorySimpleVO>>)
```json
{
  "code": 200,
  "message": "success",
  "data": [
    {
      "id": 2001,
      "name": "健身教学"
    }
  ]
}
```

---

### 2.3 获取所有启用的视频分类

#### 接口地址
```
GET /video-categories/enabled-list
```

#### 请求方式
GET

#### 请求参数
无

#### 返回结果 (ResultBean<List<VideoCategoriesVO>>)
```json
{
  "code": 200,
  "message": "success",
  "data": [
    {
      "id": 2001,
      "name": "健身教学",
      "description": "各类健身动作教学视频",
      "sortOrder": 1,
      "isEnabled": 1
    }
  ]
}
```

---

### 2.4 根据ID获取视频分类详情

#### 接口地址
```
GET /video-categories/{id}
```

#### 请求方式
GET

#### 请求参数
| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| id | long | 是 | 分类ID |

#### 返回结果 (ResultBean<VideoCategoriesVO>)
```json
{
  "code": 200,
  "message": "success",
  "data": {
    "id": 2001,
    "name": "健身教学",
    "description": "各类健身动作教学视频",
    "sortOrder": 1,
    "isEnabled": 1
  }
}
```

---

### 2.5 创建视频分类

#### 接口地址
```
POST /video-categories/create
```

#### 请求方式
POST

#### 请求参数 (VideoCategoriesCreateDTO)
| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| name | string | 是 | 分类名称 |
| description | string | 否 | 分类描述 |
| sortOrder | integer | 否 | 排序 |
| isEnabled | integer | 否 | 是否启用：0 否 1 是，默认为1 |

#### 返回结果 (ResultBean<String>)
```json
{
  "code": 200,
  "message": "分类创建成功",
  "data": null
}
```

---

### 2.6 更新视频分类

#### 接口地址
```
PUT /video-categories/update
```

#### 请求方式
PUT

#### 请求参数 (VideoCategoriesUpdateDTO)
| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| id | long | 是 | 分类ID |
| name | string | 否 | 分类名称 |
| description | string | 否 | 分类描述 |
| sortOrder | integer | 否 | 排序 |
| isEnabled | integer | 否 | 是否启用：0 否 1 是 |

#### 返回结果 (ResultBean<String>)
```json
{
  "code": 200,
  "message": "分类更新成功",
  "data": null
}
```

---

### 2.7 删除视频分类

#### 接口地址
```
DELETE /video-categories/delete/{id}
```

#### 请求方式
DELETE

#### 请求参数
| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| id | long | 是 | 分类ID |

#### 返回结果 (ResultBean<String>)
```json
{
  "code": 200,
  "message": "分类删除成功",
  "data": null
}
```

---

## 3. 状态码说明

| 状态码 | 说明 |
|--------|------|
| 200 | 操作成功 |
| 400 | 参数错误 |
| 404 | 数据不存在 |
| 500 | 服务器内部错误 |

## 4. 注意事项

1. 所有接口均采用RESTful风格设计
2. 所有请求和响应数据均为JSON格式
3. 删除操作为逻辑删除，不会真正从数据库中移除数据
4. 分类的isEnabled字段控制是否启用，0表示禁用，1表示启用
5. sortOrder字段用于控制分类在列表中的显示顺序，数值越小越靠前